import * as UE from 'ue'
import {$ref, $unref} from 'puerts'

class TS_RogueItemGA extends UE.EAB_GameplayAbility{
    //TODO add the function concat rogueItem names and trigger OnRogueItemChanged delegate.
    // the tricky part is: how to organize it so that blueprint and typescript both do what it's good at.
}

export default TS_RogueItemGA;